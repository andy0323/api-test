#!/usr/bin/env node

var fs      = require('fs'),
	request = require('request'),
	program = require('commander');
	version = require("../package.json").version;

program
	.version(version)
    	.usage("api-test .")
    	.option('-v, --verbose', '打印详细日志')
 	.parse(process.argv);

function request(URL, type, argments) {
	
	switch(type) {
		case 'get' : 
		break;

		case 'post' :
		break;

		default:
	};	

	request.post(
		{
			url : requestURL, 
			form: form
		}, 
		function(err,httpResponse,body){
			// 请求完成
			console.log(body);
		});
}

console.log("api-test start");
